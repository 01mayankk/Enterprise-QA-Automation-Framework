# Utilities Layer

This directory contains standalone helpers and system manager components.

## Contents
* `config_loader.py`: Handles configuration loading from `config/config.json`.
* `screenshot_manager.py`: Handles screenshot capture, styling/highlighting of failed elements, and directory path management.

## Usage
Import these modules as singletons or utilities in conftest or base classes to handle cross-cutting concerns.
